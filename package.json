{
  "name": "pagezone-gateway",
  "version": "1.0.0",
  "description": "Gateway worker con rate limiting y control de usage",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241127.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "private": true
}